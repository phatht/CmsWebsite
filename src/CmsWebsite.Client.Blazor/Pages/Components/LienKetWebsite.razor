
<AuthorizeView>
    <Authorized> 
        <NavLink class="nav-link" href="logout" style="float:right">
          Xin chào @context.User.Identity.Name !  Thoát <span class="oi oi-power-standby"></span>
        </NavLink>
    </Authorized>
    <NotAuthorized>
        <NavLink class="nav-link" href="login" style="float:right">
            Đăng nhập <span class="oi oi-power-standby"></span>
        </NavLink>
    </NotAuthorized>
</AuthorizeView>

@*
<AuthorizeView>
    <Authorized>
        Hello, @context.User.Identity.Name!
        <a href="logout">Log out</a>
    </Authorized>
    <NotAuthorized>
        <NavLink class="nav-link" href="login" style="float:right">
            Đăng nhập <span class="oi oi-power-standby"></span>
        </NavLink>
        <NavLink class="nav-link" href="register" style="float:right">
            Đăng kí <span class="oi oi-power-standby"></span>
        </NavLink>
    </NotAuthorized>
</AuthorizeView>*@

<RadzenTextBox Class="w-100" Placeholder="Tìm kiếm thông tin" />

<RadzenCard class="m-1">
    <h6 class="category">LIÊN KẾT WEBSITE</h6>

    <RadzenDropDown AllowClear="true" TValue="string" Class="w-100"
                    FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive"
                    FilterOperator="StringFilterOperator.Contains" AllowFiltering="true"
                    Data=@List TextProperty="Name" ValueProperty="Link" />
</RadzenCard>
@code {

    List<Item> List = new List<Item>() {
        new Item ("https://chinhphu.vn/", "Cổng thông tin điện tử chính phủ"),
        new Item ("https://dangcongsan.vn/", "Báo điện tử đảng cộng sản")
    };

    public class Item
    {
        public Item(string link, string name)
        {
            Link = link;
            Name = name;
        }
        public string Link { get; set; }
        public string Name { get; set; }

    }

    
}
